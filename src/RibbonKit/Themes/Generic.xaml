<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:RibbonKit.Controls">

    <!-- Phase 0 placeholder template. Real templates move to the token-based
         theme layer (Themes/Office2024 etc.) in Phase 1. -->
    <Style TargetType="{x:Type controls:Ribbon}">
        <Setter Property="Height" Value="118" />
        <Setter Property="Background" Value="#F3F3F3" />
        <Setter Property="BorderBrush" Value="#D1D1D1" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:Ribbon}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <TextBlock Text="RibbonKit — core ribbon skeleton coming in Phase 1"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="#8A8A8A"
                                   FontSize="13" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
